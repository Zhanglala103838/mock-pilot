auto-install-peers=true

# @hulianui 作用域包走 GitHub Packages（org=hulianui）
# 本地开发：package.json 用 link:../hulian/packages/* 软链本地源码，registry 路由不参与解析。
# 发版部署：`pnpm use:registry` 把依赖切到线上 pin 版本后，此路由 + NODE_AUTH_TOKEN 生效。
@hulianui:registry=https://npm.pkg.github.com
